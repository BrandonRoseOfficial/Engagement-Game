import { CST } from "../CST";

export class IntroScene extends Phaser.Scene{
    constructor(){
        super({
            key: CST.SCENES.INTRO
        })
    }
    create(){
        
    }
}